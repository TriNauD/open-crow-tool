# 飞书等平台 — 保姆级手动测试（评估 stub）

## 1. Stub 端点（自动化已覆盖）

```bash
curl -s -o /tmp/feishu.json -w "%{http_code}" -X POST http://localhost:3000/api/feishu/events
# 预期：501；body.code = FEISHU_NOT_ENABLED
```

## 2. 真机验证（用户待决，推荐先做）

| 步骤 | 操作 | 预期 / 记录 |
|------|------|-------------|
| 1 | Chrome 安装 future 构建的扩展并连接账号 | 正常 |
| 2 | 浏览器打开飞书**网页版**文档，划词 | 浮标是否出现？能否解释？ |
| 3 | 飞书**桌面客户端**内划词 | 通常**不可用**（记实际结果） |

若步骤 2 已覆盖需求 → **无需**上机器人/插件。

## 结论

| 日期 | Stub | 网页版扩展 | 是否继续 B/C | 备注 |
|------|------|------------|--------------|------|
| | | | | |
