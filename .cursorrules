# 团队协作协议（入口）

规则已拆分到 `.cursor/rules/`，按主题维护：
- 角色分工与输出格式
- 开发验收流程
- QA 测试流程
- Git 分支规范
- Commit 与 PR 规范
- 依赖版本与 lockfile 协作
- 任务交接与阻塞升级
- 发布与 hotfix 流程

新增或修改规则时，优先编辑对应的 `.cursor/rules/*.mdc` 文件，避免继续把所有内容堆在单一文件里。
