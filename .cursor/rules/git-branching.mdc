---
description: Git 分支策略与部署对应关系
alwaysApply: true
---

# 分支策略

| 分支 | 用途 | Vercel 部署 |
|------|------|------------|
| `main` | 生产代码，只接受来自 `dev` 的 PR | 生产环境（自动） |
| `dev` | 集成分支，功能合并到这里 | Preview 环境（自动） |
| `fea/*` / `feat/*` | 个人功能开发 | 不部署（ignoreCommand 跳过） |
| `fix/*` | Bug 修复 | 不部署 |
| `hotfix/*` | 紧急线上修复，直接向 `main` 提 PR | 不部署 |

# 功能分支命名

- 统一使用 `fea/` 前缀（owner 放末尾）。
- 格式：`fea/需求简称-<owner>`
- 当前协作人后缀：`tri`、`luna`
- 示例：`fea/weekly-digest-tri`、`fea/weekly-digest-luna`

# 日常工作流

```bash
# 1. 从最新 dev 切出功能分支
git checkout dev && git pull
git checkout -b fea/需求简称-<owner>

# 2. 开发完成后推送
git push -u origin fea/需求简称-<owner>

# 3. 向 dev 提 PR，互相 review 后合并
# 4. dev 合并后自动触发 Vercel preview，验收通过再向 main 提 PR 上线
```

# 禁止事项

- 禁止直接 push 到 `main`（hotfix 紧急除外，事后补 PR）。
- 禁止从 `main` 直接切功能分支（应从 `dev` 切）。
- AI 不主动创建 PR，除非用户明确要求。
