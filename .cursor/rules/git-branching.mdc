---
description: Git 分支策略与部署对应关系
alwaysApply: true
---

# 分支定义

| 分支 | 定位 | Vercel 部署 |
|------|------|------------|
| `main` | 生产分支，面向真实用户 | 生产环境（自动）|
| `dev` | 预览集成分支，功能在这里汇合测试 | Preview 环境（自动）|
| `fea/xxx-<owner>` | 个人功能开发 | 不部署 |
| `hotfix/xxx-<owner>` | 紧急线上修复 | 不部署 |

只有 `main` 和 `dev` 触发 Vercel 构建，其余分支全部跳过。

# 日常开发流程

```
dev  →  fea/xxx-tri (开发)  →  PR to dev  →  Vercel Preview 验收  →  PR dev→main  →  生产
```

```bash
# 每次开发前，从最新 dev 切出功能分支
git checkout dev && git pull
git checkout -b fea/需求简称-<owner>

# 开发完成，推送并向 dev 提 PR
git push -u origin fea/需求简称-<owner>
```

# 上线流程

1. `fea/*` PR 合并到 `dev`，Vercel 自动部署 Preview。
2. 在 Preview URL 上完成验收测试。
3. 验收通过，从 `dev` 向 `main` 提 PR，合并后自动部署生产。

# Hotfix 流程

```bash
# 从 main 切出 hotfix 分支
git checkout main && git pull
git checkout -b hotfix/问题简称-<owner>

# 修复完成后：
# 1. PR → main（触发生产部署）
# 2. 同步 backport 到 dev，避免下次发布时丢失修复
git checkout dev && git merge hotfix/问题简称-<owner>
```

# 禁止事项

- **默认**：禁止直接 push 到 `main` 或 **`dev`**（凡触及**业务代码、构建、依赖、CI 行为**的改动仍须 **分支 → PR → merge**）。
- **破例（仅 `dev`，非 `main`）**：单次 commit **仅**包含下列「**代码无关**」路径、且**不混入**业务源码 / `package.json`·锁文件 / `.github/workflows` / 其它影响构建或运行时的配置时，**允许**在 `dev` 上直接 **commit + push**，**无需** `fea/*` PR：
  1. **文档**：`docs/**`；`dev/**` 下需求与流程用的 **`.md`**（含 `dev/active`、`dev/done`、`dev/logs` 及目录迁移时的 `git mv`）。
  2. **仓库规则与 Cursor 技能**：`.cursor/rules/**`、`.cursor/skills/**`、`.cursorrules`；根目录 **`AGENTS.md` / `CLAUDE.md`**（若存在）。
  3. **结项收尾**（常与上两项重叠）：例如 **`需求名称-qa.md`**、**`需求名称-dev-preview-acceptance.md`**、**`dev/logs/*-log.md`**、**`tasks.md`** 勾选、**`docs/PRD.md` / `docs/PLAN.md`** 与分卷·README「进行中」等。
  **仍须走 PR 进 `dev` 的示例**：`app/`、`components/`、`lib/`、`hooks/`、`chrome-extension/src/`、`e2e/` 下测试源码、`__tests__/`、业务相关的 `.ts` / `.tsx` / `.js` / `.jsx`，以及 `package.json`、`tsconfig*`、`next.config.*`、`middleware.*`、`db/migrations/**` 等。
  **`main` 不适用直推**（上生产仍须 PR + review，见 `release-and-hotfix.mdc`）。
- 禁止从 `main` 直接切功能分支（应从 `dev` 切）。
- AI 不主动创建 PR，除非用户明确要求。
- **AI 在 `dev` / `main` 上 commit**：**不得**在 `main` 上直 commit。**`dev` 上**仅当用户要求且当次改动**全部**属于上文「代码无关 / `dev` 直推破例」、并已核对 `git diff` 无禁用路径时，可 **commit + push `origin/dev`**；其它情况须 `fea/…-<owner>`（或 `hotfix/…`）→ PR。
