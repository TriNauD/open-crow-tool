---
description: 开发流程与验收门控——所有阶段必须按顺序通过，不得跳过
alwaysApply: true
---

# 开发流程（五阶段，顺序不可跳过）

## 铁律 — 新功能、分支与「直推 dev」

- **严禁跳过前序步骤**：任何**新功能**或**业务行为变更**（特指「要做新能力」）必须按顺序走完本文阶段 **1～3** 及 `product-delivery-pipeline.mdc` 对应门控（评估 → 用户**明确**批准 → `dev/active` 立项文档与「进行中」索引，再编码）。**缺陷（Bug）** 不适用完整五阶段立项，走本文 **「Bug 修复流程」**。**不得**把「先改代码再补文档」当作常态；**不得**在未获阶段 2 批准、阶段 3 未建齐时动**新功能**相关代码。
- **严禁在 `dev` 分支上直接写业务代码**：新功能在从最新 `dev` 切出的 **`fea/需求简称-<owner>`** 上开发；**一般缺陷**在 **`bugfix/问题简称-<owner>`** 上修复（仍 **PR → `dev`**）；**生产环境紧急故障** 用 **`hotfix/问题简称-<owner>`（从 `main`）**，见 **`release-and-hotfix.mdc`**。在 `dev` 上直接 commit / push 业务源码、`package.json`、锁文件、`.github/workflows`、`db/migrations/**` 等，**一律禁止**。
- **仅「非代码资产」可按破例在 `dev` 上直推**：与 **`git-branching.mdc`**「破例」清单一致，**仅当**单次提交**全部为**规则 / Cursor 技能 / 总览与分卷文档 / `dev/**` 下流程用 `.md` 等**代码无关**路径、且不混入任何业务代码与构建配置时，**允许**在 `dev` 上直接 commit + push（**无需** `fea/*` PR）。**说明**：根目录 `notes/` 等若被 `.gitignore` 排除则**不进 Git**；需要团队可见的说明类文字请放在 **`docs/**`**（如 `docs/notes/`）。

## 功能与反馈 — 入口分流（收件箱 vs 立项）

当用户在对话中提到**功能、优化想法、产品点子**，或明确是 **Bug（缺陷）** 时：

| 入口 | 适用 | AI 动作 |
|------|------|---------|
| **Bug（缺陷）** | 现有行为错误 / 不符合预期 / 回归（**非**新能力） | **不必**走完整立项五阶段。建议 GitHub Issue（**`bug`**）可复现描述。从 **`dev`** 切 **`bugfix/<问题简称>-<owner>`** → 修 → **PR → `dev`** → Preview 验证；合并后在 **`dev/logs/`** 按 **BF-n** **存档**（见 **「Bug 修复流程」**）。**生产正在受影响**的紧急修复用 **`hotfix/*`（从 `main`）**，见 **`release-and-hotfix.mdc`**。 |
| **收件箱** | 随手记、未分拣、排期未定的念头；「先记着」「也许以后做」 | 建议或代为整理进 **`notes/需求与反馈收件箱.md`**（一行一条亦可），并提醒按该文件原则定期分拣到 **`dev/active/BRAINSTORM需求池/`** 或 GitHub Issue。**不启动**本文阶段 1～3，除非用户随后说「立项 / 按流程来 / 正式评估」。 |
| **立项** | 准备正式推进：要排期、要方案、要开发 | 从下方 **阶段 1** 起走完整流程；来自需求池时还须遵守 **`brainstorm-pool-intake.mdc`**（单独立项目录 + 回填池子 README）。 |

**[Decision] 默认追问句式（可选）**：若是缺陷且未明说，可问：**「走 `bugfix`（从 dev）还是生产救火 `hotfix`？」**；若是新点子：**「先记入需求与反馈收件箱，还是现在走立项（阶段 1）？」**

## Bug 修复流程（分支 + PR + 存档）

**与 `fea/*`（新功能）**：Bug **不要求** `dev/active` 全套 plan/context/tasks；**仍要求**独立分支、PR、测试与 **BF 存档**。

| 步骤 | 要求 |
|------|------|
| 识别 | 现象可复现；**生产已炸** → 走 **`hotfix`**，否则 **`bugfix`**。 |
| 分支 | `git checkout dev && git pull && git checkout -b bugfix/<问题简称>-<owner>` |
| 修复 | 改动 + 相关单测/E2E；合并前 **`npm run lint`** 及受影响范围 **`npm run test`**（或项目约定命令）。 |
| 合并 | **`bugfix/*` → PR → `dev`**，Preview 验收后 merge。 |
| **存档 BF** | **合并入 `dev` 之后**（或 PR 内跟 **仅文档** commit）：在 **`dev/logs/`** 写入 **BF-n**，每条至少：**现象、根因、涉及文件、验证方式**（与 `product-delivery-pipeline.mdc` 步 5 一致）。**复制粘贴模板**见 **`dev/logs/README.md`**。**优先**追加到该模块或关联需求**已有**的 `*-log.md`；没有则 **新建** `dev/logs/<模块或简述>-log.md`（或团队统一命名）。 |
| 生产 hotfix | **`hotfix/*` 从 `main`**、**PR → `main`**、再 **backport `dev`**，全文见 **`release-and-hotfix.mdc`** 与 **`git-branching.mdc`**。 |

若单 Bug 需长期拆阶段跟踪，可**另建** `dev/active/…` 立项，**分支**仍以 **`bugfix/*`** 为宜（或与 TL 约定纳入某 `fea/*`，二选一必须统一）。

## 阶段 1：需求评估（用户提出需求后）

PM 和 TL 进行评估，输出：
- `[PM]`：功能描述、用户价值、优先级
- `[TL]`：技术方案、涉及文件、风险点、工作量估算
- `[Decision]`：双方共识方案

**🚫 此阶段禁止写任何代码或改动文件。**

## 阶段 2：用户 Review（等待明确批准）

将评估结果提交给用户 review。
**必须等用户明确说"可以开始"/"通过"/"ok 开干"等确认后，才能进入阶段 3。**
用户未明确批准前，AI 不得自行推进。

用户若仅说「能不能做」「帮我实现」「直接改」等，**不视为**已阶段 2 通过；AI 仍须完成阶段 1 式讨论并**等待用户明确批准**后再进入阶段 3。

## AI 须遵守的门控（默认中文沟通 + 先文档后代码）

以下是对上文阶段的**硬性补充**（与 `product-delivery-pipeline.mdc`、`git-branching.mdc` 一致）：

1. **沟通语言**：除用户另行要求外，AI 对用户的方案、门控说明、结项与错误解释，**默认使用简体中文**。
2. **阶段 2 之前**：**禁止**修改任何**业务代码**或等效改动（见下条）。阶段 1 只允许书面讨论；若需落盘，**仅允许**「代码无关」路径（如 `docs/**`、`dev/**` 下流程用 `.md`、`.cursor/rules/**`、`.cursor/skills/**` 等，以 `git-branching.mdc` 为准），且应避免在方案未定稿时大面积改写总览 PRD。
3. **阶段 2 通过后、阶段 3 文档未齐前**：**禁止**开始本需求的编码工作。阶段 3 文档最低限度须包含：`dev/active/需求名称/` 下的 **`需求名称-plan.md`、`需求名称-context.md`、`需求名称-tasks.md`**，并更新 **`docs/product/README.md`** 与 **`docs/tech/README.md`** 的「进行中」小节（及必要时相关分卷中的一句链到该目录）。
4. **「业务代码」范围**（阶段 2 未批准或阶段 3 未建齐前不可改）：`app/`、`components/`、`lib/`、`hooks/`、业务相关 `middleware`、`db/migrations/**`、Chrome 扩展业务源码、`e2e/` / `__tests__` 中与本需求相关的**行为变更**、`package.json` / 锁文件 / `.github/workflows` 等影响构建或依赖的配置。
5. **回溯立项**：若因历史原因已**先改代码后补文档**，须在对应 **`需求名称-context.md`** 中注明为**回溯立项**，并尽快用**仅文档**提交补齐 `dev/active` 与「进行中」索引；**不得**将「先写代码再补文档」当作团队默认流程。回溯**不等价于**跳过前序步骤的许可——此后仍须遵守 **「铁律」**。

## 阶段 3：开发准备

**文档提交节奏**（与 `product-delivery-pipeline.mdc` 对齐）：用户批准总体方案后，先更新并 **commit** `docs/PRD.md` / `docs/PLAN.md`（**总览**）及本次涉及的 `docs/product/*`、`docs/tech/*` **分卷**，与下方 `dev/active`，再进入编码；**QA/手测文档**在开发方案稳定后单独立项 commit。

用户批准后，TL 建立需求文档目录：

```
dev/active/需求名称/
├── 需求名称-plan.md      # 技术方案细节
├── 需求名称-context.md   # 背景、约束、关键决策
├── 需求名称-tasks.md     # 分步任务清单（逐步打勾）
├── 需求名称-qa.md
└── 需求名称-dev-preview-acceptance.md  # 与 qa 同批；**最小路径**约一页（见「与 AI 协作」§1、§3）；合入 dev 后只更新 URL 等
```

**立项默认可发现性（与 `dev/active` 同一批或紧随其后的仅文档 commit）：**

- 更新 **`docs/product/README.md`** 与 **`docs/tech/README.md`**：增加或维护 **「进行中」** 小节（表格或列表），每条含**一行摘要** + 相对链接至 `dev/active/需求名称/`（可从仓库根路径写，如 `../../dev/active/…`）。
- 若需求主要落在一两条分卷（如仅扩展、仅认证），**同步**在对应 **`docs/product/*.md` / `docs/tech/*.md` 分卷** 加一句「进行中」并链到同一 `dev/active` 目录（可与上条合并进同一 commit）。
- **结项**（阶段 5）：将「进行中」对应行删除或改为指向 `dev/done/…`，并更新分卷定稿表述。

同时从 `dev` 切出功能分支，参照 `git-branching.mdc`。

## 阶段 4：开发与 QA 验收

- 每完成一步，立即更新 `tasks.md`。
- 上下文快用完时，先将进度和下一步写入文档，再继续。
- TL 开发完成后，QA 介入，创建或迭代 **`需求名称-qa.md`**，并确保 **`需求名称-dev-preview-acceptance.md`**（或与 qa 内 Preview 章）**已存在且结构与 `dev-workflow`「与 AI 协作」§1、§3 一致**；若阶段 3 漏建须此时补全，执行测试。
- QA 通过后，PM 审核 `tasks.md`，确认验收标准全部满足。

**🚫 QA 未通过或 PM 未审核前，不得提交代码。**

## 阶段 5：用户验收与收尾

**注**：合并到 **`dev`** 的迭代 commit / PR、Preview 多轮验收，见下文 **「与 AI 协作的交付节奏」**。本条是在**用户整体验收通过**后执行的**结项**动作。

将测试结果提交用户验收。
**必须等用户明确验收通过后，才能执行结项：**

1. 将文档迁移到 `dev/done/需求名称/`（含 plan/context/tasks/qa 与 **`需求名称-dev-preview-acceptance.md`**（若存在））。
2. 在 `dev/logs/需求名称-log.md` 记录问题与关键变更。
3. PM 更新 `docs/PRD.md`、`docs/PLAN.md` 及所涉 `docs/product/*` / `docs/tech/*` 分卷（大需求写分卷，小改可只动总览一句）；**qa / dev-preview-acceptance** 写明验收结论（PASS、日期、Preview URL 等）；**`tasks.md`** 结项勾选。
4. **`git commit` 与推送**（见 **`git-branching.mdc`**「`dev` 直推破例」）：
   - **仅文档 / 规则 / 技能 / 上述结项类 `.md`**（无业务代码、无依赖与构建配置）：**允许**在 **`dev`** 上直接 commit + **`push`**。
   - **若仍含代码或行为变更**：**fea/* → PR → `dev`**，不得夹带。

# 流程总览

```
用户提需求
    ↓
[阶段1] PM + TL 评估 → 输出方案
    ↓
[阶段2] 用户 Review → 等待明确批准 ✋
    ↓
[阶段3] TL 建文档目录 + 切分支
    ↓
[阶段4] 开发 → QA 测试 → PM 审核
    ↓
[阶段5] 用户验收 → 等待明确通过 ✋ → commit + 文档收尾
```

## 与 AI 协作的交付节奏（PR、自动化与用户验收）

与 `pr-and-commit.mdc`（含 **AI 代提 PR 闸门**）、`product-delivery-pipeline.mdc`、`git-branching.mdc`、`release-and-hotfix.mdc` **一并遵守**。本节是在五阶段之上的**操作节奏**：允许在阶段 4 内**多次 commit**、**多次 PR 进 `dev`**，在 Preview 上迭代；**阶段 5 结项与上 `main`** 仍以用户明确验收与发布门控为准。

### 1）测试文档与自动化（阶段 3 起、随开发迭代）

- 用户批准进入阶段 3 后：在 `dev/active/需求名称/` 维护 **`需求名称-qa.md`**（或等价 QA 文档），含 **用例 ID、手测步骤、自动化对表**（Vitest / `e2e/*.spec.ts` / 仅手测）。
- **与 `需求名称-qa.md` 同时**（同一 commit 或紧随其后的仅文档 commit）：须具备 **`需求名称-dev-preview-acceptance.md`**，或在 `qa.md` 内用 **独立章节「Vercel Preview（dev）验收」** 承担同一职责。该文件 **只做最小路径**（团队默认 **约一页**：**Preview URL 占位** + **前置一至三句** + **编号步骤通常 3～6 条** + **结论一行**），与 `qa.md` 里「最小路径 / §0.2」**逐条对应**即可；**迁移、DevTools、Options 扫尾、大表格式回归**等放进 **`qa.md` 或 `docs/tech/*`**，**勿**在 acceptance 里展开。须便于**单独打印**；**推荐单独文件**以免 qa 过长；可链至 **`docs/tech/environments-and-deployment.md`**，**勿**写密钥。
- **多需求同批次 / 无** `dev/active` **单需求目录时**：`dev-preview-acceptance` **一份**即可（放主需求 `dev/active/…` 或 `dev/logs/YYYY-MM-批次简称-dev-preview-acceptance.md`），勿为每个子点复制多份自相矛盾正文。
- **首个可测增量前**：为本需求补齐可运行的自动化（**优先单测**；高回归价值链路再上 Playwright）；在 QA 文档或所涉 **`docs/tech/*`** 写明 **如何运行**（如 `npm run test`、`npm run test:e2e`、`npm run test:e2e:ext`、与 CI 中扩展 build 顺序）。
- 阶段 1～2 **不写**产品代码；测试代码在阶段 3 文档就位后与实现**同步迭代**（立项瞬间不必强行写完所有 E2E，避免需求微调导致大量返工）。

### 2）合并到 `dev`（Vercel Preview）

- 功能与约定回归在 **本地/CI 通过**后，**仅当用户明确允许代开 PR** 时，AI **执行 `gh pr create --base dev`**（或用户指定的 base），返回 **PR 链接**；title/body 须符合 `pr-and-commit.mdc`；无 `gh` 时给 **compare URL**。**默认由用户 merge**。
- **提 PR 前**：须 **`npm run lint` 通过**（与 `pr-and-commit.mdc`、CI 对齐）；详见下文「全绿」约定。
- 代提前：**merge `origin/dev` 入当前功能分支**、工作区已提交、可 **`push`**；详见 `pr-and-commit.mdc`。

### 3）Preview 手测（合入 `dev` 后：**更新**验收 MD，而非从零写）

- **文档应在立项/写 `qa.md` 时已具备**（见 §1）：`需求名称-dev-preview-acceptance.md` 或 `qa.md` 内「Vercel Preview（dev）验收」章，且含 **URL 占位**。
- **触发**：对应功能 **已合并进 `dev`**、Vercel **Preview 部署就绪**后（用户告知或请 AI 跟进），AI 须 **打开已有文件**：
  - **替换** Preview **URL 占位**为本次 Deployment 真实链接（或注明从 Vercel Dashboard 哪条部署取）；
  - **补充**仅与本次环境相关的一两句（如 Deployment Protection、某变量变更后需 Redeploy）；
  - 若需求范围变化，**同步**改正文步骤表，并与 `需求名称-qa.md` **互链或对齐**。
- **疏漏补救**：若立项时未生成该文件，须在 **合入 `dev` 后立刻补全**（仍放 `dev/active/需求名称/` 或按 §1 之 `dev/logs/` 规则），并在 log 中记一笔疏漏。
- **正文须满足**（首版即定稿；合 dev 后只做 **URL / 一句环境说明**）：**最小路径**清单——
  - **环境**：Preview URL 占位 + **极短前置**（扩展 dist、Protection/变量类 **链文档** 即可，勿在此抄长表）。
  - **步骤**：仅保留**不可替代的手测**编号流（通常合并网站与扩展为一条线，**不**要求大表、**不**要求「可选旁证」段；自动化命令放 `qa.md`）。
  - **结论**：**一行** PASS/FAIL（+ 备注）。
- **勿**把密钥写进该 MD。

### 4）缺陷与需求变更

- 用户指出问题或变更范围：为对应 **case** **补/改自动化**（或更新手测条目）→ AI 修复并跑通约定门禁（`lint`、`test`、本需求涉及的 **`test:e2e`** 等）→ **再交用户验收**（可多轮）。
- 根因与验证写入 **`dev/logs/需求名称-log.md`**（可与 `product-delivery-pipeline` 的 BF-n 记录方式一致）。

### 5）结项与 `dev` → `main`

- **仅当用户明确说验收通过**后：执行阶段 5 清单（`dev/done/`、log、`docs/PRD.md` / `docs/PLAN.md` 及分卷等）。
- **`dev` → `main`**：**另开 PR**；须用户**明确授权**后 AI 方可代开；合并、上线与发布后观察遵守 **`release-and-hotfix.mdc`**。
- **Hotfix**：不走过本条主线时，按 **`release-and-hotfix.mdc`**：`hotfix/*` → `main`，再 **backport** `dev`。

### 「全绿」约定

合并 `dev` 前，至少 **CI 或与 CI 等价的本地命令**通过，且顺序建议与 `.github/workflows/ci.yml` 一致：

1. **`npm run lint`**（必跑；提 PR 前门槛，避免仅跑测试而漏掉与 CI 不同步的 rules）
2. **`npm run test`**
3. **`npm run build`**（与 CI 相同占位 env 时更贴近）
4. **`npm ci` + `npm run build`**（在 `chrome-extension/`）再 **`npm run test:e2e`**（扩展/页面 E2E 相关改动时）

**扩展 / 划词相关改动**须 **先构建 `chrome-extension/dist`** 再跑 Playwright；详见 `docs/tech/chrome-extension-e2e-and-regression.md`。
