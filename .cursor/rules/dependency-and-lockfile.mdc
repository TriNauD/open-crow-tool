---
description: 依赖版本与 lockfile 协作规则
alwaysApply: true
---

# 运行时对齐

- 团队统一 Node `20.20.2` 与 npm `10.8.2`。
- 使用 `.nvmrc` 切版本，避免本地环境漂移。
- CI 与 Vercel 的 Node 版本需与项目约定一致。

# 依赖变更

- 只有在“明确要改依赖”时才允许更新 `package-lock.json`。
- 改依赖的 PR 必须在描述里注明新增/升级/删除包及原因。
- 禁止把依赖升级和业务功能混在同一个 PR。

# 日常安装

- 拉取最新代码后优先执行 `npm ci`。
- 如遇 lockfile 冲突，先同步 `main` 再重新安装并生成干净 lock。
