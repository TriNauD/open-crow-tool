---
description: 环境变量与密钥管理规范
alwaysApply: true
---

# 基本原则

- `.env.local` 和所有 `.env.*` 文件绝对不进 Git，已在 `.gitignore` 保护。
- 唯一进 Git 的是 `.env.local.example`（占位符，无真实值）。
- 新增环境变量时，必须同步更新 `.env.local.example` 并在 PR 里说明用途。

# 密钥共享方式

- 不得通过微信/飞书/邮件明文传递密钥。
- 双人团队推荐：使用同一个 Bitwarden / 1Password 共享保险库，或直接通过 Vercel Dashboard 同步。
- Vercel 生产密钥由项目 Owner 管理，协作者通过 Vercel 界面直接读取，不本地存储。

# 泄漏处理

- 发现密钥泄漏（误提交、截图曝光等），立即执行：
  1. 在对应平台（Supabase / Resend / OpenAI）吊销旧 key，生成新 key。
  2. 更新 Vercel 环境变量。
  3. 通知另一人同步本地 `.env.local`。
  4. 用 `git filter-repo` 或联系 GitHub 清除历史记录（如已推送）。

# 变量命名

- 全大写 + 下划线：`SUPABASE_URL`、`RESEND_API_KEY`。
- 按服务前缀分组：`SUPABASE_*`、`RESEND_*`、`OPENAI_*`、`DIGEST_*`。
- 禁止用 `TOKEN`、`KEY`、`SECRET` 等过于通用的裸名，必须加服务前缀。
