---
description: Commit 与 PR 轻量规范
alwaysApply: true
---

# Commit 规范

- 每个 commit 只做一件事（功能、重构、文档不要混在一起）。
- 推荐前缀：`feat:` `fix:` `chore:` `refactor:` `docs:` `test:`。
- 提交信息写“为什么改”，避免只写“改了什么”。

# PR 规范

- **`dev` 直推（代码无关）**：**仅** `docs/**`、`.cursor/rules/**`、`.cursor/skills/**`、`.cursorrules`、`AGENTS.md`/`CLAUDE.md`、`dev/**` 下**纯 `.md` 流程文档**（含结项迁移、qa、acceptance、log、tasks 等）时，**允许**直接在 **`dev`** 上 commit 并 push，**无需** PR；**禁止**与业务代码、依赖或 CI 配置混在同一 commit。完整允许/禁止清单见 **`git-branching.mdc`**。
- 小步提交，小 PR 优先（建议 < 300 行核心改动）。
- PR 描述至少包含：背景、改动点、测试结果、回滚方式。
- 有 UI 改动时附截图；有接口改动时附请求示例。
- **AI 撰写 PR 正文**：须满足上文「背景、改动点、测试、回滚」等要点；**在已由 AI 执行 `gh pr create` 的场景下**，仍以聊天或 PR 页为准查阅即可，**不**要求用户从聊天复制大段 Markdown。
- **仅当无法代开时**（无 `gh`、未 `gh auth login`、非 GitHub 远程、`gh` 报错且短时无法修复）：再输出 **compare URL** + **完整 Markdown 正文**（单个 `markdown` 代码块或逐行可粘贴文本），供用户在浏览器里建 PR。

## AI 代提 PR（闸门与默认分支）

- **默认不主动开 PR**：未得到用户示意前，**不要**自行 `gh pr create`。**用户一旦明确要求**——包括口头说「提 PR / 开 PR / 合并进 dev / dev 合 main / 直接提不要我只复制」等——即视为授权；AI **应**在终端完成 **push（若尚未推送）+ `gh pr create`**，并把 **`gh pr view --web` 或命令输出里的 PR 链接**发回用户。
- **默认 target 分支**（除非用户另行指定）：
  - 当前功能分支（`fea/*`、`hotfix/*` 等）→ **`dev`**
  - **`dev` → `main`（生产）**：仅在用户明确要求发布 / 合并到 `main` 时操作；仍须遵守 `release-and-hotfix.mdc` 与双人 review。
- **代提时默认动作顺序**（本地已安装且已登录 `gh` 为前提）：
  1. 完成「代提前检查」与 **`npm run lint`**（见下）。
  2. **`git push -u origin <current-branch>`**（若远程尚无该分支）。
  3. 执行 **`gh pr create --base <base> --head <head> --title "…" --body-file <path>`**（或 `--body` + heredoc）；正文字段须符合「PR 规范」。
  4. 将 **`gh pr create` 返回的 URL**（或 `gh pr view`）告知用户。
  **备选**：环境无 `gh` 时，才退化为 compare URL + 可粘贴正文（见上文）。
- **代提前检查**：合并最新 **`origin/dev`**（或用户指定的 base）进当前分支、工作区已提交、远程可 `push`；勿强推公共分支。
- **提 PR 前 ESLint（强制）**：在仓库根执行 **`npm run lint`**，**直至退出码 0**。与 CI 第一步一致；**未通过不得** `push`/`gh pr create`（避免仅跑单测或 E2E 而 CI 卡在 lint）。**纯文档 / 规则 / `dev` 直推类 commit 且明确不涉及前端源码时**，经用户确认可 **跳过** lint（须在 PR 正文中注一句「本次未改业务代码，未跑 lint」）。

### 本地安装 GitHub CLI（`gh`）以便代提 PR

适用于本机终端执行；AI 在已安装且已登录的环境中可直接 `gh pr create`。

**macOS（Homebrew）**

```bash
brew install gh
gh auth login
```

按提示选择：GitHub.com → HTTPS → 浏览器登录或粘贴 token（勿把 token 贴进聊天）。

**验证**

```bash
gh auth status
cd /path/to/open-crow-tool && gh pr view 2>/dev/null || true
```

**由 AI 代开 PR 到 `dev` 的典型命令**（在仓库根、已 push 当前分支后）

```bash
gh pr create --base dev --head "$(git branch --show-current)" \
  --title "…" \
  --body "$(cat <<'EOF'
…PR 正文 Markdown…
EOF
)"
```

若标题/正文较长，也可用 `gh pr create --base dev` 进入交互向导。合并方式仍按团队约定在 GitHub UI 选择 **Create a merge commit**。

# 合并规则

- 至少 1 人 review 通过再合并（双人团队互审）。
- **优先 `Create a merge commit` 合并 PR**（保留分支上的真实提交历史，避免 squash 后基线漂移、反复冲突、强推改历史带来的协作成本）。
- 仅在「单 commit 小修」或双方明确约定时，才用 `squash merge`。
- 合并前先 **merge `dev` 进功能分支**（或 rebase，二选一但团队统一即可），确保本地验证通过后再提 PR；**不要**依赖 squash 来“消冲突”。
