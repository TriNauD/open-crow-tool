---
description: Commit 与 PR 轻量规范
alwaysApply: true
---

# Commit 规范

- 每个 commit 只做一件事（功能、重构、文档不要混在一起）。
- 推荐前缀：`feat:` `fix:` `chore:` `refactor:` `docs:` `test:`。
- 提交信息写“为什么改”，避免只写“改了什么”。

# PR 规范

- 小步提交，小 PR 优先（建议 < 300 行核心改动）。
- PR 描述至少包含：背景、改动点、测试结果、回滚方式。
- 有 UI 改动时附截图；有接口改动时附请求示例。
- **AI 代写 PR 描述时**（须遵守，便于用户粘贴到 GitHub）：
  - 输出 **Markdown 源码**（含 `#`、`-`、行内代码、链接等），保证**复制后进 GitHub PR 编辑框格式仍正确**。
  - **不要**以「预览态」为唯一交付：不要只口头列结构；不要只靠聊天里的渲染预览充当正文（复制时常丢语法）。用户要能 **一整段可复制**。
  - **推荐**：用**单个** fenced 代码块，首行为 `markdown`，内层即完整 PR 正文；用户只复制代码块内部（不含围栏）粘贴即可。
  - 若不用代码块，则逐行输出未渲染的 Markdown 文本，并明确写一句「以下为可直接粘贴进 PR 的 Markdown」。

## AI 代提 PR（闸门与默认分支）

- **默认不代开 PR**：仅当用户**明确说**可以提 PR（例如「可以提 PR」「帮我代开 PR 到 dev」）时，AI 才在终端执行 `git push` / `gh pr create` 或等价操作。**禁止**在用户未开口的情况下主动创建 PR。
- **默认 target 分支**（除非用户另行指定）：
  - 当前功能分支（`fea/*`、`hotfix/*` 等）→ **`dev`**
  - **`dev` → `main`（生产）**：仅在用户明确要求发布 / 合并到 main 时操作；仍须遵守 `release-and-hotfix.mdc` 与双人 review。
- **代提时 AI 须交付**：符合上文「PR 规范」的 **title** + **body**（推荐可复制的 `markdown` 代码块）；若环境无 `gh`，同时给出 **compare URL**（`https://github.com/<owner>/<repo>/compare/<base>...<head>`）供浏览器创建。
- **代提前检查**：合并最新 **`origin/dev`**（或用户指定的 base）进当前分支、工作区已提交、远程可 `push`；勿强推公共分支。

### 本地安装 GitHub CLI（`gh`）以便代提 PR

适用于本机终端执行；AI 在已安装且已登录的环境中可直接 `gh pr create`。

**macOS（Homebrew）**

```bash
brew install gh
gh auth login
```

按提示选择：GitHub.com → HTTPS → 浏览器登录或粘贴 token（勿把 token 贴进聊天）。

**验证**

```bash
gh auth status
cd /path/to/open-crow-tool && gh pr view 2>/dev/null || true
```

**由 AI 代开 PR 到 `dev` 的典型命令**（在仓库根、已 push 当前分支后）

```bash
gh pr create --base dev --head "$(git branch --show-current)" \
  --title "…" \
  --body "$(cat <<'EOF'
…PR 正文 Markdown…
EOF
)"
```

若标题/正文较长，也可用 `gh pr create --base dev` 进入交互向导。合并方式仍按团队约定在 GitHub UI 选择 **Create a merge commit**。

# 合并规则

- 至少 1 人 review 通过再合并（双人团队互审）。
- **优先 `Create a merge commit` 合并 PR**（保留分支上的真实提交历史，避免 squash 后基线漂移、反复冲突、强推改历史带来的协作成本）。
- 仅在「单 commit 小修」或双方明确约定时，才用 `squash merge`。
- 合并前先 **merge `dev` 进功能分支**（或 rebase，二选一但团队统一即可），确保本地验证通过后再提 PR；**不要**依赖 squash 来“消冲突”。
