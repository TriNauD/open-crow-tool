---
description: QA 介入时机与分级回归策略
alwaysApply: true
---

# QA 流程

触发时机：
- **新功能**：TL 完成开发后，QA 介入并创建 `dev/active/需求名称/需求名称-qa.md`。
- **Bug 修复**：可无独立 `qa.md`；须在 PR 描述或 **`dev/logs/*-log.md` BF-n** 中写清复现、验证方式（见 **`dev-workflow.mdc`「Bug 修复流程」**）。

QA 文档（新功能）应包含：
- 影响域标注（涉及模块 + 风险等级）
- 功能测试（新功能）
- 回归测试（受影响模块）
- 测试结论（PASS / FAIL + 遗留问题）

# 分级回归策略

- **核心链路（每次发布必跑）**：
  - 订阅流程（`POST /api/subscribe` -> DB 写入）
  - 退订流程（`GET /api/unsubscribe` -> status = cancelled）
  - 邮件发送（`sendMail` 基础连通）
- **邻域测试**：仅改动波及模块时执行。
- **纯新增功能**：只跑新功能用例，不强制全量旧回归。
- **low-risk 快速通道**：纯样式/文案/注释可降级为 smoke test。

# 自动化与回归库

- **单元/契约**（Vitest）：`__tests__/**/*.test.ts`，不启动浏览器，跑 `npm run test`。
- **浏览器冒烟（E2E）**（建议 Playwright，可选阶段）：`e2e/` 或 `tests/e2e/`，用例与 `需求名称-qa.md` 的 **§0.2** 中 `E2E-xx` 编号**对表**；流程与落地方见项目技能 `qa-e2e-smoke`（`.cursor/skills/qa-e2e-smoke/SKILL.md`）。手测中无法自动化的项仍须在 §0.2 注明原因。
- 维护 `dev/regression.md`（如存在），记录核心链路及自动化覆盖状态。
