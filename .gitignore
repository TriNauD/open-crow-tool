# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-report/
# 通配：Playwright 默认输出 test-results/，跑 E2E 时常用 --output=test-results-xxx
# 换目录绕过沙箱对 test-results/ 的批量清理限制，故一并忽略
/test-results*/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
**/.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
*.pem

# Cursor local (ignore direct children; allow rules/ and skills/ directories through)
.cursor/*
!.cursor/rules/
!.cursor/skills/

# Claude local (skills moved to .cursor/skills/)
.claude/

# Zcode local (session plans)
.zcode/

# Editor local settings (personal, not for repo)
# To share recommended extensions, un-ignore .vscode/extensions.json
.vscode/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (do not commit secrets; example file is tracked)
.env
.env.*
!.env.local.example
!chrome-extension/.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# personal notes (local only — do not commit). 对外简明用户说明见 docs/notes/
/notes/
